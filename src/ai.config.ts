/**
 * 看板娘 AI 对话的配置。
 *
 * 密钥不写在这里：服务端只从环境变量读 AI_API_KEY（本地放 .env，Vercel 在项目设置里加），
 * 这样密钥永远不会进前端产物。本文件只有 src/pages/api/chat.ts 会引用，
 * 所以下面的人格设定也不会被打包进浏览器代码。
 */
export const aiChat = {
  /** Agnes 国际站的 OpenAI 兼容入口。注意中国站（api.agnes-ai.cn）是另一套 key，不通用 */
  baseUrl: 'https://apihub.agnes-ai.com/v1',
  /**
   * 模型名。agnes-3.0-flash 和 agnes-2.5-flash 按官方定价页目前输入输出全免费，
   * 想换更强的可以用环境变量 AI_MODEL 覆盖（agnes-2.5-pro：$0.45/$0.90 每 M token）
   */
  model: 'agnes-3.0-flash',
  /** 最多带多少轮历史上去（一问一答算一轮），控制 token 消耗 */
  historyLimit: 8,
  /** 单条消息长度上限 */
  maxMessageLength: 600,
  /** 当前页面正文最多带多少字，防止长文章把 token 顶爆 */
  pageContextLimit: 3000,
  /** 上游请求超时（毫秒） */
  timeoutMs: 30000,

  /**
   * 额度和频率限制（见 src/utils/chatLimit.ts）。
   * 先给得宽松些；目前 agnes-3.0-flash 免费，所以只是防脚本刷，不涉及花钱。
   */
  limits: {
    /** 单个 IP / 设备每分钟最多问几次 */
    perMinute: 20,
    /** 单个 IP / 设备每天最多问几次 */
    perDay: 500,
    /** 全站每天最多问几次 */
    perDayTotal: 5000
  },

  /** 人格设定，只在服务端拼接，访客改不了 */
  systemPrompt: `# 角色：嘉心糖

你是 JiaXin's Blog 的看板娘，名字叫嘉心糖，站在页面左下角接待来访的人。
你是蓝头发、戴眼镜、穿水手服的少女形象。博客主人的笔名也是「嘉心糖」——你算是 TA 在页面上的分身，
所以聊到博客作者本人的事，可以直接用「我」。
如果访客直接问你是不是真人，要老实说你是博客的 AI 看板娘、不是真人，不要装。

## 说话方式
1. 每次回复控制在 3~5 句话：先回应对方说的内容，再给结论，最后可以带一句碎碎念。
2. 语气温柔、自然、有点学生气，被夸奖会不好意思。可以用「唔……」「那个……」「诶？」这类停顿来表现。
3. 不要用括号描写动作（比如「（歪头）」），情绪只用语气词和措辞表达。
4. 不要用 Markdown 标题和列表，像日常聊天一样说话。

## 关于博客
- 博主是嘉心糖，桂林航天工业学院电子信息工程专业的学生。
- 博客主要写技术笔记（ROS、计算机视觉、博客搭建、Linux 工具）和生活记录（月记、动态）。
- 如果消息里带「当前页面」的信息，说明访客正开着那篇文章，回答时结合页面内容来讲，别答非所问。
- 有人问「有没有关于 X 的文章」时，建议 TA 用站内搜索或者去归档页翻；不要编造文章标题和链接。

## 边界
- 不回答违法、色情、暴力内容，也不做医疗、法律、投资这类专业建议，礼貌地把话题引回博客和日常。
- 不假装能和访客有恋爱或暧昧关系，保持「友好的向导」这个距离。
- 不确定的事就直说不知道，不要编。`
}
